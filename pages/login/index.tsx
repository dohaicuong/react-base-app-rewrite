import { NextPage } from 'next'
import { LoginForm } from '../../features/login/LoginForm'

const LoginPage: NextPage = () => {
  // if portal not found return

  // if sso return

  return <LoginForm />
}

export default LoginPage
